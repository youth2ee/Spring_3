<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="qnaMapper">

	<select id="qnaList" parameterType="com.naver.s3.util.Pager" resultType="com.naver.s3.qna.QnaVO">
		select * from (select N.*, rownum R from (select * from qna order by num desc) N)
		where R between #{startRow} and #{lastRow}
	</select>

	<select id="qnaCount" resultType="java.lang.Integer">
		select count(nvl(num,0)) from qna
	</select>

</mapper>